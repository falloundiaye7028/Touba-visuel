name: Deploy Bijouterie Alpha

on:
  push:
    branches: [main, claude/setup-video-project-sMuGg]
    paths:
      - 'bijouterie-alpha/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: bijouterie-alpha/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: bijouterie-alpha

      - name: Build static site
        run: npm run build
        working-directory: bijouterie-alpha

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: bijouterie-alpha/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4
