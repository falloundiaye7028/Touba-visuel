'use client';
import {useState} from 'react';
export default function Login(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);return <main className="workspace" style={{maxWidth:500,paddingTop:70}}><a className="brand" href="/">KIIRAAY INFOS</a><section className="panel" style={{marginTop:30}}><h1 style={{fontSize:27}}>Connexion administrateur</h1><form className="form-grid" style={{marginTop:25}} onSubmit={async e=>{e.preventDefault();const form=e.currentTarget;setBusy(true);setError('');try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:new FormData(form).get('password')})});const body=await response.json();if(!response.ok)throw Error(body.error);window.location.assign('/gestion');}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}><label className="field">Mot de passe<input type="password" name="password" autoComplete="current-password" required maxLength={256}/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="primary" disabled={busy}>{busy?'Connexion…':'Se connecter'}</button></form></section></main>;}
